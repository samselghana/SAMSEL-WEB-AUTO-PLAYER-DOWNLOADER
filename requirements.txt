# Web server (required)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# AutoMix / Downloader engine (required for /api/automix; Docker/Railway install all of this)
mutagen>=1.47.0
yt-dlp>=2024.8.0
syncedlyrics>=1.0.0
numpy>=1.24.0,<3
librosa>=0.10.0
soundfile>=0.12.0
